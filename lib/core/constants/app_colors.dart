import 'package:flutter/material.dart';

// Clase para almacenar los colores de nuestra paleta de diseño
class AppColors {
  // --- Colores Principales ---

  // Azul oscuro/índigo para AppBar, DrawerHeader, etc.
  static const Color fondoPrincipal = Color(0xFF1F2937);

  // Fondo principal oscuro (Gris azulado)
  static const Color fondoSecundario = Color(0xFF1F2937);

  // Naranja vibrante para botones y acentos
  static const Color acentoCTA = Color(0xFFFF9800);

  // --- Colores de Texto y Secundarios ---
  static const Color textoPrincipal = Color(0xFFFFFFFF); // Blanco
  static const Color textoBoton =
      Color(0xFF1F2937); // Texto para botones naranjas
  static const Color textoSecundario =
      Color(0xFFBDBDBD); // Gris para subtítulos

  // Fondo del menú lateral y modales
  static const Color fondoDrawer = Color(0xFF263238);
}
